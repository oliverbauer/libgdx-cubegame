package io.libgdx.cubegame.blocks.types;

import io.libgdx.cubegame.blocks.Block;

/**
 * A transport block moves from position p1 to p2 (always stopping for a few seconds), 
 * a player could move on the transport block (if target pos is not null and the TileTransporter is not moving)
 */
public class TileTransporter extends Block {
	// TODO implement me
}
